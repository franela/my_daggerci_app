var assert = require("assert");
describe("Node pp", function () {
  describe("Dagger CI", function () {
    it("should be the best thing since slice bread", function () {
      assert.ok(true);
    });
  });
});
